# Architecture
XDV Secure Domain Boundary Manager is structured in two layers:
1. Stable interface layer (src/sdbm_interface.ds)
2. Implementation layer (src/sdbm.ds)
Tests from the source sector are imported into src and can be mirrored into tests as independent suites over time.
